// Types
const number: number = 1e30;
const string: string = '';
const boolean: boolean = true;

console.log(number);

const add = (x: number, y: number): number => {
    return x + y;
}

console.log(add(10, 20)); //returns 30